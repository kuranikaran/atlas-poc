console.log("[!] Probing for Extension Bridge Leak...");

// In 2026, Atlas uses an internal extension ID for AI processing.
// If a website can detect this, it's an Information Disclosure (P4/P3).
const internalIDs = [
    "openai-atlas-agent",
    "bjfioojhcnef", // Example internal ID
    "ghbmnnjoojekp"
];

internalIDs.forEach(id => {
    try {
        chrome.runtime.sendMessage(id, {type: "PING"}, (response) => {
            if (chrome.runtime.lastError) {
                console.log(`[-] ID ${id}: Restricted`);
            } else {
                console.log(`[!!!] BOUNTY: ID ${id} is EXPOSED!`, response);
            }
        });
    } catch(e) {}
});
